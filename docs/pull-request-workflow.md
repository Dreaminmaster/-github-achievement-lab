# Pull request workflow practice

This exercise records a simple pull-request lifecycle:

1. Create a focused branch from the current default branch.
2. Make one coherent change.
3. Open a pull request with a clear summary and reason.
4. Verify the changed files and commit scope.
5. Merge using an appropriate strategy.

For small documentation-only updates, squash merging keeps the default branch history concise.
